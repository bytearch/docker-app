{
 beats {
     port => 5044
 }
}
output {
 elasticsearch {
     hosts=>["http://172.30.1.52:9200"]
     index=>"%{[@metadata][beat]}-%{[@metadata][version]}-%{+YYYY.MM.dd}"
     #user=>"elastic"
     #password=>"bytearch"
 }
}
